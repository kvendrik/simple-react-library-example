{
  "name": "simple-react-library-example",
  "version": "1.0.0",
  "description": "",
  "main": "dist/src/index.js",
  "scripts": {
    "clean": "rm -rf ./dist || true",
    "build": "yarn clean && tsc",
    "playground": "yarn build && cd playground && yarn build && yarn dev"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@shopify/typescript-configs": "^3.0.2",
    "@types/react": "^16.9.56",
    "@types/react-dom": "^16.9.9",
    "serve-dev": "^1.0.0-alpha.6",
    "typescript": "^4.0.5"
  },
  "dependencies": {
    "react": "^17.0.1"
  },
  "engines": {
    "node": ">13"
  }
}
